import { styled } from '@/styles'

export const Main = styled('main', {
  gridArea: 'content',
  display: 'flex',
  flexDirection: 'column',
  flexWrap: 'nowrap',
  alignContent: 'center',
  justifyContent: 'center',
  alignItems: 'center'
})
