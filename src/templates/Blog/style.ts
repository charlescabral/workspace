import { styled } from '@/styles'

export const Div = styled('div', {
  gridArea: 'content',
  display: 'flex',
  flexDirection: 'column',
  flexWrap: 'nowrap',
  alignContent: 'center',
  justifyContent: 'center',
  alignItems: 'center',
  minHeight: 68
})
