import { Meta } from '@storybook/react'
import SwitchTheme from '.'

export default {
  title: 'SwitchTheme',
  component: SwitchTheme
} as Meta

export const Basic = {
  args: {
    text: 'SwitchTheme'
  }
}
