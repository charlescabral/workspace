import { styled } from '@/styles'

export const Div = styled('div', {
  display: 'block',
  border: 'dashed 2px $text',
  padding: '2rem 6rem',
  textAlign: 'center',
  margin: '2rem',
  borderRadius: 14
})
