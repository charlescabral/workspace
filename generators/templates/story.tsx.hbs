import { Meta } from '@storybook/react'
import {{pascalCase name}} from '.'
import { {{pascalCase name}}Props } from './type'

export default {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}}
} as Meta<{{pascalCase name}}Props>

export const Basic = {
  args: {
    text: '{{pascalCase name}}'
  }
}
